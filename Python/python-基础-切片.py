----------------------------
切片						|
----------------------------
	# 针对于,有角标的数据类型的一种操作
	# 可以进行CRUD等操作
		* 不可修改的数据类型,不能进行修改,删除操作
		* str,tuple

	# 切片的特性
		* [x:y:z]

		* 留头不留尾
		* x默认是第一位,y默认最后一位
		* -1,表示最后一位的角标
		* 切片返回的是新的集合

		* z,默认是1,表示跨级,不能为0
		* 如果z是负数,则表示为把切片后的结果逆序
			x: 表示最右边
			y: 表示最左边
		


		

