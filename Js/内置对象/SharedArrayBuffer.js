-------------------
SharedArrayBuffer
-------------------
	# 表示一个通用的原始二进制数据缓冲区，类似于 ArrayBuffer 对象，但它可以用来在共享内存上创建视图。
		* 与可转移的 ArrayBuffer 不同，SharedArrayBuffer 不是可转移对象。

		* SharedArrayBuffer 与 ArrayBuffer具有同样的API。
		* 二者的主要区别是 ArrayBuffer 必须在不同执行上下文间切换，SharedArrayBuffer 则可以被任意多个执行上下文同时使用。

-------------------
this
-------------------

-------------------
static
-------------------
