----------------------
WeakSet
----------------------
	# WeakSet是Set的“兄弟”类型，其API也是Set的子集。
		* WeakSet中的“weak”（弱），描述的是JavaScript垃圾回收程序对待“弱集合”中值的方式。
		* 元素只能是 Object、继承 Object 的类型和非全局注册的 Symbol，尝试添加非法元素设置值会抛出TypeError。
	

----------------------
this
----------------------

----------------------
static
----------------------