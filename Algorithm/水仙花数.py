# 所谓水仙花数就是一个三位数,它各位的立方之和加起来的数值等于本身,
# 153就是一个水仙花数
    153 = 1 + 125 + 27

	1 * 1 * 1 = 1
	5 * 5 * 5 = 125
	3 * 3 * 3 = 27




def flowers():
    x = 0
    y = 0
    z = 0
    for i in range(100, 1000):
		# 第一位
        x = i // 100
		# 第二位
        y = (i % 100) // 10
		# 第三位
        z = (i % 100) % 10
        if (x*x*x + y*y*y + z*z*z) == i:
            print(i)