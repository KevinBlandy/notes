-----------------------------
C 内置函数					 |
-----------------------------
sizeof()
	* 以字节为单位,返回对象的大小
		printf("int 的大小是 %d",sizeof(int));  //4
	* 返回类型是 size_t
	* 如果参数是关键字,必须添加括号,如果是变量,则不需要
		int num = 4;
		printf("size = %d",sizeof num);


