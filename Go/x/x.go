
--------------
x
--------------
	# x包
		https://pkg.go.dev/golang.org/x
	
		* 这些包是 Go 项目的一部分，但在 Go 主树之外。它们是在比 Go 核心更宽松的兼容性要求下开发的。使用“go get”安装它们。
	
	# 包含的库
		benchmarks — 衡量 Go 开发的基准。
		blog — blog.golang.org的实现。
		build — build.golang.org的实现。
		crypto — 额外的加密包。
		debug — Go 的实验性调试器。
		image — 额外的成像包。
		mobile——在移动 平台上对 Go 的实验性支持。
		net — 额外的网络包。
		perf — 用于性能测量、存储和分析的包和工具。
		pkgsite — pkg.go.dev 网站的主页。
		review — 一个处理 Gerrit 代码审查的工具。
		sys ——额外的并发原语。
		sys — 用于进行系统调用的包。
		text — 处理文本的包。
		time — 额外的时间包。
		tools ——godoc、goimports、gorename 和其他工具。
		tour - tour.golang.org的实现。
		exp — 实验性和不推荐使用的包（小心处理；可能会在没有警告的情况下更改）。